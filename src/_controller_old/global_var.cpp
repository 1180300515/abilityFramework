#include <string>
#include <controller/global_var.h>

// define the global var

// the cloud server address
std::string cloud_address = "";
// the http sever address
std::string http_server_address = "http://0.0.0.0:8000";
// the device hostname
std::string device_hostname = "";

